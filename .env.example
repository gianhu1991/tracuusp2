# Backend tra cứu (chạy Playwright). Khi deploy Vercel, thêm vào Environment Variables.
BACKEND_URL=https://your-backend.railway.app
AUTHORIZATION=Bearer your-token-here

# Để Cursor/script có thể push code lên GitHub, thêm vào .env.local (không commit):
# GITHUB_TOKEN=ghp_xxxxxxxx
# Tạo token: GitHub → Settings → Developer settings → Personal access tokens (quyền repo).
